# hometown-today Vite production build 환경변수
# .env.production 에 복사해서 사용.

# 데이터 서빙 API 도메인 (Cloudflare Workers with Assets)
VITE_API_BASE=https://api.ryanpp.com

# 앱인토스 콘솔 > 인앱 광고 > 광고 그룹 에서 발급받은 실제 ID
# 테스트 ID (`ait-ad-test-banner-id`) 는 심사에서 반려되므로 실 ID 필수
VITE_AD_GROUP_ID_BANNER=
