{
  "name": "hometown-today",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build && ait build",
    "preview": "vite preview",
    "deploy": "ait deploy"
  },
  "devDependencies": {
    "@apps-in-toss/devtools": "^3.2.0",
    "typescript": "~6.0.2",
    "vite": "^8.2.2"
  },
  "dependencies": {
    "@apps-in-toss/web-framework": "^3.4.0"
  }
}
